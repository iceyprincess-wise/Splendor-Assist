<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.assistant.adapter.watchdog">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_extension"
        android:label="Splendor Watchdog Adapter"
        android:hardwareAccelerated="true">
        
        <service
            android:name=".WatchdogAdapterService"
            android:exported="true"
            android:process=":kernel_watchdog"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                      android:value="Supervises adapter health, failures and recovery telemetry."/>
        </service>
    </application>
</manifest>
